import Foundation

extension Render {
    struct Thumbnail: RenderCommand {
        func output(for url: URL, with: Render.Pub) {
            // await publisher.received(output: output)
        }
    }
}
